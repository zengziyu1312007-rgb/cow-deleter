# 牛来清理文件 · Agent 说明

## 一句话安装口令

- 用户说「帮我安装牛来清理文件」「装一下牛来清理文件」或同义表达时，在 macOS 上直接执行 `./install.sh`。
- 安装成功后，确认 `~/Applications/NiuLaiCleaner.app` 和 `~/Library/Services/牛来清理文件.workflow` 均存在，再告诉用户从 Finder 的「右键 → 快速操作 → 牛来清理文件」启动。
- 不要为了验证而清理用户的真实文件；需要演示时，只能新建可丢弃的测试文件。
- Windows 暂不执行该脚本，按 `README.md` 的 Windows 构建说明处理。
